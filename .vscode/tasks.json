{
    "version": "2.0.0",
    "tasks": [
        {
            
            "label": "Build",
            "linux": {
                "command": "make build"
            },
            "windows": { 
                "type": "shell",
                "command": "bash -c 'make build'"
            }
        },{
            "label": "Build and clean",
            "linux": { 
                "command": "make build_clean"
            },
            "windows" : {
                "type": "shell",
                "command": "bash -c 'make build_clean'"
            }
        }
    ]
}